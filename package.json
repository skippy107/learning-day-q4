{
  "name": "learning-q4",
  "version": "1.0.0",
  "description": "Simple static API to use with Docker inside of a Jenkins pipeline",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint --fix src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "skippy107",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^6.6.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2"
  }
}
